# Backend environment
PORT=3001

# Postgres (docker-compose)
# If you use the docker-compose.yml at repo root, the DB runs on localhost:5432
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/art_sell?schema=public"
